---
:hierarchy:
    - %{::customer_name}/%{::customer_site}/%{::customer_build}
    - %{::customer_name}/%{::customer_site}
    - %{::customer_name}
    - common
:backends:
    - yaml
:yaml:
    :datadir: '/etc/puppet/hieradata'